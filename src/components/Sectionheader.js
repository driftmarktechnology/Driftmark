function Sectionheader({title, subtitle}){
    return(
        <div class="section-title">
          <span>{title}</span>
          <h2>{title}</h2>
          <p>{subtitle}</p>
        </div>
    )
}
export default Sectionheader;